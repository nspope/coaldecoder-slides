# insert relate slide in showing coalescence rates through time
# to say that "this has been done before, but what about ... there's no simple bijection"

# transition to CTMP slides needs work 

# this should be a caveats / opportunities slide
# mention that one big advantage of fitting models in the first
# place is that it allows simulation; this gives a mechanism to check
# against fit; mention time windows

# add slide with inference machinery, bootstrapping, etc

